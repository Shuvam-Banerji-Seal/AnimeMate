<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        Package visibility (Android 11+, API 30+).

        By default, apps on API 30+ can only see other apps that
        match a declared <queries> entry, are in the same package
        signature, or are explicit OS exemptions. Without this,
        PackageManager.queryIntentActivities() for the https
        scheme returns empty even when Chrome / Firefox / Samsung
        Internet is installed — and OAuth flows that bounce out
        to the system browser break silently.

        We declare the two query intents we actually need:
          1. https:// (browsers) — for the OAuth launcher's
             "is there a browser?" check, and any future code
             that needs to know which apps handle http(s).
          2. animerec:// (our own scheme) — for the OAuth callback
             activity's intent-filter matching.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="animerec" android:host="auth" />
        </intent>
    </queries>

    <application
        android:name=".AnimeRecApp"
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnimeRec">
        
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AnimeRec">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            OAuth callback activity — receives the deep-link redirect from
            the system browser (or any other app that resolves
            `animerec://auth`) after the user logs in to MAL and clicks
            "Allow". Sits at singleTask with no UI, then hands control
            back to MainActivity.
        -->
        <activity
            android:name=".ui.auth.OAuthCallbackActivity"
            android:exported="true"
            android:theme="@style/Theme.Translucent.NoTitleBar"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:taskAffinity="">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="animerec"
                    android:host="auth" />
            </intent-filter>
        </activity>
        
        <!-- FileProvider for sharing error log files via email -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
    </application>

</manifest>